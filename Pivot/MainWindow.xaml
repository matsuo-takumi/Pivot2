<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Pivot.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pivot"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Pivot"
    xmlns:viewmodels="using:Pivot.ViewModels"
    xmlns:media="using:Microsoft.UI.Xaml.Media"
    xmlns:converters="using:Pivot.Converters">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid x:Name="AppTitleBar" Background="Transparent" Grid.Row="0" Height="32">
            <TextBlock x:Name="AppTitleBarText"
                       Text="Pivot"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"
                       Style="{StaticResource CaptionTextBlockStyle}"/>
        </Grid>
        
        <!-- Main Pivot Control -->
        <Pivot x:Name="MainPivot" 
               Grid.Row="1"
               SelectionChanged="MainPivot_SelectionChanged"
               IsHeaderItemsCarouselEnabled="False">
            <Pivot.Resources>
                <Style TargetType="PivotHeaderItem">
                    <Setter Property="Padding" Value="12,0,12,0"/>
                    <Setter Property="Margin" Value="0,0,8,0"/>
                    <Setter Property="MinHeight" Value="0"/>
                </Style>
            </Pivot.Resources>
            <Pivot.LeftHeader>
                <Grid Width="18"/>
            </Pivot.LeftHeader>
            <PivotItem Header="Home">
                <Frame x:Name="HomeFrame" Background="Transparent"/>
            </PivotItem>
            <PivotItem Header="Asset">
                <Frame x:Name="AssetFrame" Background="Transparent"/>
            </PivotItem>
            <PivotItem Header="Image">
                <Frame x:Name="ImageFrame" Background="Transparent"/>
            </PivotItem>
            <PivotItem Header="Project">
                <Frame x:Name="ProjectFrame" Background="Transparent"/>
            </PivotItem>
            <PivotItem Header="Code">
                <Frame x:Name="CodeFrame"/>
            </PivotItem>

            <PivotItem Header="Preference">
                <Frame x:Name="PreferenceFrame"/>
            </PivotItem>
        </Pivot>
        
        <!-- Loading Indicator (bottom-right) -->
        <Border x:Name="LoadingIndicator"
                Grid.Row="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,16,16"
                Padding="12,8"
                CornerRadius="8"
                Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <ProgressRing IsActive="True" Width="16" Height="16"/>
                <TextBlock x:Name="LoadingText" 
                           Text="Loading..." 
                           VerticalAlignment="Center"
                           Style="{StaticResource CaptionTextBlockStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
