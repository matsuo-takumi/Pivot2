<Page
    x:Class="Pivot.Views.AssetPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pivot.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button Content="Scan" Command="{Binding ScanCommand}" IsEnabled="{Binding IsScanning, Converter={StaticResource BooleanNegationConverter}}"/>
            <Button Content="Cancel" Command="{Binding CancelScanCommand}" IsEnabled="{Binding IsScanning}"/>
        </StackPanel>

        <ProgressBar Grid.Row="1" Height="4" Minimum="0" Maximum="100" Value="{Binding Progress}"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="6">
            <TextBlock Text="Progress:"/>
            <TextBlock Text="{Binding Progress}"/>
            <TextBlock Text="%"/>
        </StackPanel>

        <ListView Grid.Row="3" ItemsSource="{Binding Files}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Width="400" Text="{Binding Path}"/>
                        <TextBlock Width="120" Text="{Binding Type}"/>
                        <TextBlock Width="120" Text="{Binding Size}"/>
                        <TextBlock Width="220" Text="{Binding UpdatedAt}"/>
                        <TextBlock Width="320" Text="{Binding Hash}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
