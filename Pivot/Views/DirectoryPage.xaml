<Page
    x:Class="Pivot.Views.DirectoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pivot.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Pivot.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    x:DataType="local:DirectoryPage"
    x:Name="DirectoryPageRoot">

    <ScrollViewer>
        <StackPanel Padding="20" Spacing="20">
            <!-- Asset Directories Section -->
            <StackPanel Spacing="8">
                <TextBlock Text="Asset Directories" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <ListView ItemsSource="{x:Bind ViewModel.ScanDirectories, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <StackPanel Orientation="Horizontal" Spacing="8" Padding="0,4">
                                <TextBlock Text="{Binding}" VerticalAlignment="Center" HorizontalAlignment="Left" Width="350"/>
                                <Button Content="−" Command="{Binding DataContext.ViewModel.RemoveDirectoryCommand, ElementName=DirectoryPageRoot}" CommandParameter="{Binding}" CornerRadius="4" Padding="8,4"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBox x:Name="AssetDirText" Width="350" PlaceholderText="Enter directory path..."/>
                    <Button Content="Browse" Click="BrowseButton_Click" Tag="AssetDirText" CornerRadius="4" Padding="8,4"/>
                    <Button Content="+" Command="{x:Bind ViewModel.AddAssetDirectoryCommand}" CommandParameter="{Binding ElementName=AssetDirText, Path=Text}" CornerRadius="4" Padding="8,4"/>
                </StackPanel>
            </StackPanel>

            <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,8"/>

            <!-- Image Directories Section -->
            <StackPanel Spacing="8">
                <TextBlock Text="Image Directories" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <ListView ItemsSource="{x:Bind ViewModel.ScanDirectories, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <StackPanel Orientation="Horizontal" Spacing="8" Padding="0,4">
                                <TextBlock Text="{Binding}" VerticalAlignment="Center" HorizontalAlignment="Left" Width="350"/>
                                <Button Content="−" Command="{Binding DataContext.ViewModel.RemoveDirectoryCommand, ElementName=DirectoryPageRoot}" CommandParameter="{Binding}" CornerRadius="4" Padding="8,4"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBox x:Name="ImageDirText" Width="350" PlaceholderText="Enter directory path..."/>
                    <Button Content="Browse" Click="BrowseButton_Click" Tag="ImageDirText" CornerRadius="4" Padding="8,4"/>
                    <Button Content="+" Command="{x:Bind ViewModel.AddImageDirectoryCommand}" CommandParameter="{Binding ElementName=ImageDirText, Path=Text}" CornerRadius="4" Padding="8,4"/>
                </StackPanel>
            </StackPanel>

            <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,8"/>

            <!-- Project Directories Section -->
            <StackPanel Spacing="8">
                <TextBlock Text="Project Directories" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <ListView ItemsSource="{x:Bind ViewModel.ScanDirectories, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <StackPanel Orientation="Horizontal" Spacing="8" Padding="0,4">
                                <TextBlock Text="{Binding}" VerticalAlignment="Center" HorizontalAlignment="Left" Width="350"/>
                                <Button Content="−" Command="{Binding DataContext.ViewModel.RemoveDirectoryCommand, ElementName=DirectoryPageRoot}" CommandParameter="{Binding}" CornerRadius="4" Padding="8,4"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBox x:Name="ProjectDirText" Width="350" PlaceholderText="Enter directory path..."/>
                    <Button Content="Browse" Click="BrowseButton_Click" Tag="ProjectDirText" CornerRadius="4" Padding="8,4"/>
                    <Button Content="+" Command="{x:Bind ViewModel.AddProjectDirectoryCommand}" CommandParameter="{Binding ElementName=ProjectDirText, Path=Text}" CornerRadius="4" Padding="8,4"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>


