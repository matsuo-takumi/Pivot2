<UserControl
    x:Class="Pivot.Views.FilterSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:Pivot.ViewModels"
    xmlns:converters="using:Pivot.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:DataType="viewmodels:FilterSettingsViewModel"
    Background="Transparent">

    <UserControl.Resources>
        <converters:BooleanNegationConverter x:Key="BooleanNegationConverter" />
    </UserControl.Resources>

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="{Binding FilterTypeDisplayName}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button Content="Add Filter" Click="AddFilter_Click" Margin="12,0,0,0"/>
            <Button Content="Restore Defaults" Command="{Binding RestoreDefaultsCommand}" Margin="8,0,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,12,0,0" Padding="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
            <ScrollViewer>
                <ItemsControl ItemsSource="{Binding Filters}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:FilterSettingsViewModel+FilterItemViewModel">
                            <Border Padding="8" Margin="0,0,0,8" Background="#10FFFFFF" CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding AllowedExtensionsString}" FontSize="12" Foreground="{ThemeResource AppTextCaptionBrush}" TextWrapping="Wrap" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                        <ToggleSwitch IsOn="{Binding Visible, Mode=TwoWay}" Toggled="ToggleVisibility_Toggled" Tag="{Binding}"/>
                                        <Button Content="Edit" Click="EditFilter_Click" Tag="{Binding}"/>
                                        <Button Content="Delete" Click="DeleteFilter_Click" Tag="{Binding}" IsEnabled="{Binding IsBuiltIn, Converter={StaticResource BooleanNegationConverter}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>

