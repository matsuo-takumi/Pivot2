<Page
    x:Class="Pivot.Views.ImagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pivot.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー: 表示モード切替 -->
        <StackPanel Orientation="Horizontal" Spacing="8" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Images" FontSize="20" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" Spacing="4" Margin="12,0,0,0">
                <Button Content="Grid"/>
                <Button Content="List"/>
            </StackPanel>
        </StackPanel>

        <!-- 本体: グリッド表示（ImageItemにバインド） -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsRepeater ItemsSource="{Binding Images}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemWidth="220" MinItemHeight="160" MinRowSpacing="8" MinColumnSpacing="8"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="6" Margin="4">
                            <StackPanel>
                                <Grid Width="200" Height="120" Background="#f2f2f2">
                                    <Image Stretch="Uniform" Source="{Binding ThumbnailPath}"/>
                                </Grid>
                                <TextBlock Text="{Binding Name}" FontSize="12" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
