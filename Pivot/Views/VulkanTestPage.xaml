<Page
    x:Class="Pivot.Views.VulkanTestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrls="using:Pivot.Controls"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#20E0F7FF" CornerRadius="6" Padding="12" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBlock Text="Vulkan Renderer Test" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Text="Phase 2: Clear Color Verification" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- Vulkan Viewport -->
        <Border Grid.Row="1" Background="#20000000" CornerRadius="6" Padding="0">
            <ctrls:VulkanViewerControl x:Name="VulkanViewer" MinHeight="400"/>
        </Border>
    </Grid>
</Page>
