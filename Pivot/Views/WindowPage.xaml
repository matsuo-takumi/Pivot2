<Page
    x:Class="Pivot.Views.WindowPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pivot.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Pivot.Converters"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="20">
        <StackPanel Spacing="20">
            <StackPanel Spacing="10">
                <TextBlock Text="Menu Display Mode" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <RadioButtons Header="Choose a menu display mode" SelectedItem="{Binding MenuDisplayMode, Mode=TwoWay}" ItemsSource="{Binding MenuDisplayModes}">
                    <RadioButtons.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </RadioButtons.ItemTemplate>
                </RadioButtons>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
